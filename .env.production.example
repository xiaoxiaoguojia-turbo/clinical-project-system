# ============================================
# 临床创新项目管理系统 - 生产环境配置模板
# Ubuntu 24.04 + x86架构
# ============================================
# 
# 使用说明：
# 1. 复制此文件为 .env.production
# 2. 修改所有配置值
# 3. 确保 JWT_SECRET 和数据库密码足够复杂
# 4. 不要将 .env.production 提交到Git
#
# ============================================

# ===== MongoDB 数据库配置 =====
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=请修改为强密码-至少16位-包含大小写字母数字特殊字符
MONGO_DATABASE=clinical_project_system

# Next.js连接MongoDB的URI（Docker内部网络）
# 格式: mongodb://用户名:密码@主机:端口/数据库?authSource=admin
MONGODB_URI=mongodb://admin:请修改为强密码@mongodb:27017/clinical_project_system?authSource=admin

# ===== JWT 安全配置 =====
# 用于生成和验证JWT token的密钥（必须修改！）
JWT_SECRET=请修改为复杂的密钥-至少32位随机字符串-使用openssl-rand-base64-32生成

# ===== 文件上传配置 =====
# 单个文件最大尺寸（字节）100MB = 104857600
MAX_FILE_SIZE=104857600
# 上传目录（容器内路径，通过Docker卷持久化）
UPLOAD_DIR=/app/uploads

# ===== 应用配置 =====
# API基础URL（生产环境改为实际域名）
API_BASE_URL=http://localhost:3000
# 应用名称
NEXT_PUBLIC_APP_NAME=临床创新项目管理系统
# 应用版本
NEXT_PUBLIC_APP_VERSION=1.0.0

# ===== Coze AI 配置 =====
# 需要外网访问，部署时开启网闸
COZE_API_KEY=your-coze-api-key-here
COZE_BOT_ID=your-bot-id-here
COZE_WORKFLOW_ID=your-workflow-id-here

# ===== 可选配置 =====
# Node.js 内存限制（MB）
NODE_OPTIONS=--max-old-space-size=2048

# 时区设置（东八区）
TZ=Asia/Shanghai

# ============================================
# 安全建议：
# 1. 使用 openssl rand -base64 32 生成JWT_SECRET
# 2. 数据库密码至少16位，包含大小写字母、数字、特殊字符
# 3. 定期更换密码和密钥
# 4. 不要在代码或日志中暴露敏感信息
# ============================================
